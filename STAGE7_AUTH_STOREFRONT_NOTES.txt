LeadLens Stage 7 Auth + Storefront Patch
Version: 1.7.0

Included:
- Secure login beta path on Login screen
  - Email/password sign-in
  - Email/password sign-up
  - Forgot password trigger
  - Continue with Google button
  - Continue with Microsoft button
- Supabase client updated for persistent mobile auth sessions
- New auth utility for email/OAuth/reset flows
- Manager-triggered password reset action in Admin view
- Storefront scan reliability improvements
  - saves recent storefront scan history locally
  - captures GPS-backed reverse geocode context
  - prefers nearby business geocode and GPS state over weak OCR state guesses
  - stores OCR summary/debug clues on storefront-derived leads
  - marks weak storefront address results for review instead of pretending certainty
- Expo app scheme set to leadlens for auth deep links
- Version bumped to 1.7.0

Important:
- OAuth will not work until Google/Microsoft providers are configured in Supabase and the redirect URLs are set correctly.
- This patch adds the app-side plumbing and UI. Provider setup is still required.
- After applying, run npm install to pull the added Expo auth/browser dependencies.
